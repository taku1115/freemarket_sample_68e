.productLists
  - @items.zip(@item_images) do  |a,b|
    .productList
      = link_to image_tag(b["image_url"],class: "productList__img"),item_path(a)
      - if a["trading_status"] ==0
        .soldout
          .soldout__text SOLD OUT 
      .productList__body
        %h3.name
        = a.name
        .details
          %ul 
            %li 
              = a.price
            %h5 円
            %li
              %i.fa.fa-star.likeIcon (税込)
            %h6 👍9670